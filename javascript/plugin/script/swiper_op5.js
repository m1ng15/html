const newSlide = new Swiper('.new_slide_wrap',{
    //속성, 값
    mousewheel:true,
    direction: 'vertical',
    //수직 이동시 mousewheel 필수 설정
    // 수직 이동시 autoplay(자동전환), loop(무한반복) 설정 X
});

const snsSlide = new Slide('.sns_portfolio',{

});